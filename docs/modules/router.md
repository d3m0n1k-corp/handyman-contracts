# Router Context

No main router file exists in this repository yet.

When a main router file is added, update this file and `docs/modules.md` with its path. Use that router file as context for future requests involving:

- Service registration
- Route wiring
- Middleware
- Authentication interception
- Module exposure

